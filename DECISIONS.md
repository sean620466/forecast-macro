# Decision Log

## D-001 — 분석 우선 MVP
- 상태: Accepted
- 결정: 첫 버전은 자동거래 없이 자체 확률과 시장가격 차이를 보여준다.
- 이유: 데이터와 모델의 정확도 검증을 먼저 수행하기 위해서다.

## D-002 — 공식 데이터 우선
- 상태: Accepted
- 결정: FRED, BLS, BEA 및 Federal Reserve 원자료를 우선 사용한다.
- 이유: 출처 추적성과 수정 이력 관리가 중요하다.

## D-003 — 신호 임계값
- 상태: Provisional
- 결정: 절대 확률 차이 8%p 이상을 기본 표시 기준으로 둔다.
- 이유: 초기에는 작은 노이즈를 사용자 신호에서 제외한다.
- 재검토: 백테스트와 예측시장 수수료 분석 후 조정한다.

## D-004 — 협업 규칙
- 상태: Accepted
- 결정: Claude 검산 결과는 `reviews/`에, 최종 결정은 이 파일에 기록한다.

## D-005 — 캘리브레이션 전 사용자 신호 금지
- 상태: Accepted
- 결정: baseline 모델은 기본적으로 `should_display=false`를 반환한다.
- 이유: 표본외 캘리브레이션이 완료되지 않은 확률을 투자 신호처럼 노출하지 않는다.

## D-006 — 백테스트 입력의 vintage 필수
- 상태: Accepted
- 결정: 공식 백테스트는 예측시점 당시의 ALFRED/FRED vintage만 사용한다.
- 이유: 사후 수정된 경제 데이터로 인한 미래정보 누출을 차단한다.

## D-007 — 캘리브레이션 완료 기준
- 상태: Accepted
- 결정: 최소 30개 표본외 예측에서 시장가격 baseline 대비 Brier Skill Score가 0보다 커야 신호 자격을 검토한다.
- 이유: 절대 Brier score만으로는 모델이 시장보다 나은지 판단할 수 없다.

## D-008 — 정답 확정 전 학습 금지
- 상태: Accepted
- 결정: 시간순 검증에서도 테스트 예측시각 전에 결과가 확정된 학습 행만 사용하고 필요시 embargo를 적용한다.
- 이유: 순서가 앞선 행이라도 정답 발표가 늦으면 미래정보 누출이 발생한다.

## D-009 — 발생지 시간대 보존
- 상태: Accepted
- 결정: FOMC와 BLS 발표시각은 `America/New_York`으로 생성하고 비교 시 UTC로 변환한다.
- 이유: 일광절약시간에 따른 UTC 한 시간 오차를 방지한다.

## D-010 — 시장가격 정규화 기준
- 상태: Accepted
- 결정: 동일시각의 mid 가격과 완전한 outcome 집합만 정규화하며 합계 오차 5% 초과 시 거부한다.
- 이유: 불완전 계약과 비정상 스프레드를 가짜 확률분포로 만드는 것을 막는다.

## D-011 — 제로금리 하한 결과공간 제약
- 상태: Accepted
- 결정: 목표범위 상단이 0.25% 이하이면 인하 확률을 구조적 epsilon 0.005로 고정한다.
- 이유: 연준의 비음수 정책금리 제약 아래 불가능에 가까운 결과에 높은 확률을 주지 않는다. epsilon은 백테스트에 맞춰 보정하지 않는다.

## D-012 — 연구 게이트와 사용자 신호 분리
- 상태: Accepted
- 결정: climatology 및 always-hold 대비 성능은 연구용 게이트로만 기록한다. 시장가격 baseline이 없으면 `signal_eligible`은 항상 false다.
- 이유: D-007의 최종 기준은 시장보다 나은 표본외 Brier 성능이기 때문이다.

## D-013 — 비-ZLB 유효 표본 기준
- 상태: Accepted
- 결정: 모델 연구 게이트의 최소 30건은 목표범위 상단 0.25% 초과인 표본으로 계산한다.
- 이유: ZLB에서 얻은 구조적 이득을 경기순환 예측력으로 오인하지 않기 위해서다.

## D-014 — 결과 확정 시각은 공식 발표 일정 기준
- 상태: Accepted (2026-09-08, 사용자 승인)
- 결정: 계약의 `outcome_at`은 `data/release_schedule.csv`(BLS·연준 공식 일정, `America/New_York`)에서만 생성한다. 거래소가 보고하는 마감·정산 시각은 참고용이며, 거래소 마감이 공식 발표 이후이면 계약을 거부한다. 일정 CSV의 각 행은 출처 URL과 수집 시각을 보존한다.
- 이유: Polymarket `endDate`는 ET 벽시계를 UTC로 표기하는 등 시간대가 신뢰할 수 없다(리뷰 5 H-3). 결과를 알고 거래할 수 있는 창을 구조적으로 막는다.

## D-015 — 얇은 구간 시장의 확률 정규화
- 상태: Accepted (2026-09-08, 사용자 승인). D-010의 mid 합 ±5% 규칙을 대체한다.
- 결정: 상호배타 구간 계약의 YES 호가에 대해 (1) `Σbid ≤ 1 ≤ Σask`(허용 오차 0.01)이면 완전 시장으로 인정하고, (2) `Σask − Σbid`가 0.35를 넘으면 거부하며, (3) 각 구간의 확률을 `[bid, ask]` 범위와 함께 기록하고, 점추정은 mid에서 초과분(`Σmid − 1`)을 스프레드 비례로 덜어낸 값으로 한다. 이 점추정은 항상 각 구간의 `[bid, ask]` 안에 있고 합이 정확히 1이다.
- 이유: 꼬리 구간의 넓은 스프레드가 mid 합을 부풀려(실측 1.14) 정상 시장이 거부됐다(과제 08). 매수호가 합이 1에 가깝다는 것이 완전성의 올바른 증거이며, 확률을 범위로 보존하면 스프레드 불확실성이 사라지지 않는다.

## D-016 — Fed 모델 결과공간은 인하 / 동결 / 인상 3원
- 상태: Accepted (2026-09-08, 사용자 승인)
- 결정: Fed 모델은 `cut`, `hold`, `hike` 세 확률(합 1)을 낸다. 백테스트·실시간 비교·채점은 3원 벡터를 기록하고, 결과별 이진 Brier와 3원 Brier를 함께 보고한다. 기존 이진 지표의 P(cut)은 3원의 cut 성분과 동일해야 한다(중첩). 시장 baseline은 Kalshi 사다리에서 같은 세 결과와 `[bid, ask]` 범위로 만든다. ZLB 마스크(D-011)는 cut에만 적용한다.
- 이유: 시장은 인상 가능성을 별도로 가격에 반영하는데(2026-09-08 실측 0.525) 이진 모델은 이를 표현할 수 없어 정면 비교가 불가능했다(R14-M2). 표본에 인상 11회가 있어 학습 가능하다.
- 구현 방식(1단계): 인하는 기존 모델, 인상은 "인하가 아닐 때" 조건부 이진 모델. 다항 로지스틱은 표본이 늘어난 뒤 재검토.

## D-017 — 예측 cutoff와 시장 관측시각 정렬
- 상태: Accepted (2026-09-08, 사용자 승인)
- 결정: 백테스트 cutoff는 회의 전날(D−1) vintage를 유지한다. 회의 당일 08:30 ET에 CPI 또는 고용보고서 발표가 있는 회의는 `same_day_release=true`로 표시하고, 시장 대비 채점에서는 "시장 정보우위" 회의로 별도 집계한다. 실시간 비교는 모델 계산 시각과 시장 관측 시각을 기록하고, 채점은 결정 전 마지막 기록만 사용한다.
- 이유: 시장이 알고 모델이 모르는 정보로 인한 불리함을 숨기지도 과장하지도 않는다.

## D-019 — 사다리 유동성 게이트를 rung 기준으로
- 상태: Accepted (2026-09-08, 사용자 승인)
- 결정: 누적 임계값 사다리(Kalshi)의 유동성 게이트를 배타 구간의 폭이 아니라 rung의 호가 스프레드로 판정한다. 평균 rung 스프레드 ≤ 0.05, 최대 rung 스프레드 ≤ 0.12. 만기 완화는 D-018과 같은 방식으로 결과까지 남은 개월이 2를 넘을 때 개월당 +0.01, 상한 각 0.10 / 0.20. 배타 구간의 `[bid, ask]` 범위와 폭은 그대로 기록하되 게이트에 쓰지 않는다. 구간 시장(Polymarket YES/NO)은 D-015 그대로. 기록에 `mean_rung_spread`, `max_rung_spread`와 그 한도를 남기고, 평균 스프레드가 0.05를 넘으면 `low_liquidity=true`로 표시해 채점에서 분리한다.
- 이유: 배타 구간 하나의 범위는 인접 rung 두 개의 스프레드를 합친 것이라 폭은 대략 `2 × Σ(rung 스프레드)`가 되어, 폭 게이트는 호가가 아니라 단 수를 재고 있었다(과제 37). 0.1%p 간격의 실업률·CPI 사다리(14~26단)는 호가가 Fed 사다리보다 좁은데도 전부 거부됐다.
- 결과: D-018의 폭 상한은 기록용으로만 남는다.

## D-018 — 먼 만기 사다리의 폭 상한
- 상태: Accepted (2026-09-08, 사용자 승인)
- 결정: 누적 임계값 사다리(Kalshi)에 한해 D-015의 폭 상한을 `min(0.60, 0.35 + 0.05 × max(0, 결과까지 남은 개월 − 2))`로 완화한다. 폭이 0.35를 넘는 스냅샷은 `low_liquidity=true`로 표시하고 채점에서 별도 집계한다. 점추정과 범위 계산은 D-015 그대로다.
- 이유: 12개월 이상 만기 사다리는 꼬리 호가가 넓어 전부 거부됐다. 유동성 낮은 기록을 따로 표시하면 표본을 넓히면서 해석은 보존한다.
